# Variables

## What is a Variable?

A variable is a name that refers to an object in memory.

Example:

name = "John"

The variable name points to the string object "John".

## Important Points

- Uses = 
- Can be reassigned
- Variables are references
- Python is dynamically typed